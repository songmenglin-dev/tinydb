"""Version string for the tinydb package.

Kept in a tiny module so the version is importable without pulling in any
of the heavier subpackages (storage / sql / executor / etc.).
"""

__version__ = "0.1.0"
