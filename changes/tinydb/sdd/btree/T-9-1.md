# T-9.1 — README + examples

## Goal
Project-facing documentation: `README.md` (install + quick-start + API overview + limitations) and `examples/demo.py` (runnable end-to-end demo).

This is NOT a code-test. Verification: `python examples/demo.py` exits 0; smoke-tested manually + an automated subprocess test in B9.3.

## Carry-forward NITs

- `tinydb.open(path)` is the public entry (T-7.1).
- 7 / 8 REPL works (T-8.3 / T-8.4).
- TypeTag system: INT/FLOAT/TEXT/BOOL/DATE/TIME/DATETIME/DECIMAL/BLOB/JSON.
- Aggregation: COUNT, SUM, AVG, MIN, MAX + GROUP BY.
- ACID via WriteLock + WAL + Recovery (B6).

## Files to Create / Modify

### `/mnt/c/sml/project/py_project/tinydb/README.md` (~120 lines)

Sections (sequential):
1. Title + 1-line tagline
2. Features (the 7 capabilities from proposal.md)
3. **Install** — `pip install tinydb` (placeholder — we're zero-dep, so it's `git clone` + `python -m pytest`). Document.
4. **Quick Start** — 5-line snippet:
   ```python
   import tinydb
   with tinydb.open("/tmp/demo.db") as db:
       db.execute("CREATE TABLE users (id INT PRIMARY KEY, name TEXT NOT NULL)")
       db.execute("INSERT INTO users VALUES (1, 'alice')")
       for row in db.execute("SELECT * FROM users"):
           print(row)
   ```
5. **CLI** — `python -m tinydb --db /tmp/demo.db -c "SELECT 1"` and REPL basics.
6. **Limitations / Out of Scope** — explicit list (JOIN, concurrency, ALTER, views, triggers, FKs, network).
7. **Architecture overview** — 1-paragraph summary referencing storage / sql / executor / tx / index modules.
8. **Running tests** — `python -m pytest tests/`.
9. **License** — MIT.
10. **Status** — v0.1.0.

### `/mnt/c/sml/project/py_project/tinydb/examples/demo.py` (~80 lines)

A runnable end-to-end demo:
1. Open a temp DB.
2. CREATE TABLE `users` + `orders`.
3. INSERT 5 users and 3 orders.
4. SELECT — print all rows.
5. CREATE INDEX on `users(name)`.
6. SELECT WHERE name = 'alice' — print.
7. UPDATE age on a row.
8. Aggregate: COUNT, AVG per group.
9. Transaction: BEGIN / INSERT / COMMIT.
10. Cleanup: DROP TABLE, close DB.

This file is also the entry point for B9.3's automated end-to-end test.

## No Tests Required

(B9.1 doesn't add a pytest test — verification is "demo.py runs".)

## Mechanical Verification

- `python examples/demo.py` exits 0.
- File appears in `git status`.

## Out of Scope (T-9.1)

- Building infrastructure (CI, Docker, etc.).
- API stability commitments.
- PyPI upload.

## Report
`/mnt/c/sml/project/py_project/tinydb/changes/tinydb/sdd/btree/T-9-1-report.md`.
