# T-8.5 — `__main__` + entry point

## Goal
`python -m tinydb` works. Routes argv through the argparse → either runs one SQL or enters REPL.

## File
`src/tinydb/__main__.py` (~30 lines)

```python
import sys
from tinydb.cli import main
sys.exit(main())
```

## Tests Required
`tests/cli/test_main_e2e.py` — uses `subprocess.run`:

1. `python -m tinydb --db /tmp/x.db -c 'CREATE TABLE t (id INT PRIMARY KEY)'` → exit 0.
2. Same with follow-up `-c 'INSERT INTO t VALUES (1)'` → exit 0, row appears.
3. `python -m tinydb --db /tmp/x.db -c 'SELECT * FROM t'` → exit 0, output contains '1' (or formatted result).
4. Invalid SQL via -c → exit nonzero + ParseError message on stderr.
5. `--help` → exit 0 + help text.
6. Missing --db → exit 2 (argparse error).

## TDD.
